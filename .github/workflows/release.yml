name: Release

on:
  push:
    branches: [main]
    paths:
      - com.narlei.gpucputemperature.ulanziPlugin/manifest.json

permissions:
  contents: write

jobs:
  release:
    # macOS runner: `make package` compiles the native Swift thermal helper
    # and cross-publishes the .NET Windows helper (self-contained, no
    # Windows runner needed).
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read version from manifest
        id: version
        run: echo "version=$(jq -r .Version com.narlei.gpucputemperature.ulanziPlugin/manifest.json)" >> "$GITHUB_OUTPUT"

      - name: Check if release already exists
        id: check
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "v${{ steps.version.outputs.version }}" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Release v${{ steps.version.outputs.version }} already exists — skipping."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
          fi

      - uses: actions/setup-node@v4
        if: steps.check.outputs.exists == 'false'
        with:
          node-version: '20'

      - uses: actions/setup-dotnet@v4
        if: steps.check.outputs.exists == 'false'
        with:
          dotnet-version: '8.0.x'

      - name: Build plugin ZIP
        if: steps.check.outputs.exists == 'false'
        run: make package

      - name: Create release
        if: steps.check.outputs.exists == 'false'
        env:
          GH_TOKEN: ${{ github.token }}
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          gh release create "v$VERSION" \
            --repo "$GITHUB_REPOSITORY" \
            --title "v$VERSION" \
            --generate-notes \
            --latest \
            dist/com.narlei.gpucputemperature.ulanziPlugin.zip
